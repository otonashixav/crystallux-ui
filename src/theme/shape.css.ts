import { createThemeContract } from "@vanilla-extract/css";

export const shapeVars = createThemeContract({
  none: null,
  xs: null,
  sm: null,
  md: null,
  lg: null,
  xl: null,
  full: null,
});
